<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrmikeonabike.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".book_details"></activity>
        <activity android:name=".player" />
        <activity android:name=".grid_view" />
        <activity android:name=".travel" />
        <activity android:name=".technology" />
        <activity android:name=".sci_fi" />
        <activity android:name=".romance" />
        <activity android:name=".classics" />
        <activity android:name=".biographies" />
        <activity android:name=".ListView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>